// null_statement.cpp
char* myStrCpy(char* Dest, const char* Source)
{
    char* DestStart = Dest;

    // 将 Source 指向的值赋值给
    // Dest 直到字符串 0 的结尾是
    // encountered.
    while (*Dest++ = *Source++)
        ;   // 空语句。

    return DestStart;
}

int main04021()
{

    return 0;
}